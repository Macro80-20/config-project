name: Feature
on: 
 push:
  branches-ignore:
  - master
  - main
  - development
jobs:
  write-to-console:
    name: Push feature branch
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Push to feature branch!'